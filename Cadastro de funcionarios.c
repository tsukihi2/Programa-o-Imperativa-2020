#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
 int main (){

     int func[4];
     int contf =0;
    char respostacad;
                scanf("%d",&contf);
                    if (contf >=4) {printf ("maior q 4");}//para limitar o numero de funcionarios
                    //{
                       // do
                       // {

                          //  printf("Bem vindo ao cadastro de usuario\n");
                          //  printf("Digite os numeros da matricula\n");
                          //  scanf("%d", &func[contf]);  // usando vetor eu consigo variar a posição com um loop usando a variavel
                           // printf("Numero da matricula %d \n" , func[contf]);
                          //  contf++;
                           // printf("Digite 1 para cadastrar outro ou 2 para sair\n");
                           // scanf ("%c", &respostacad);
                           // printf("%c", respostacad); // confirmacao da resposta
                       // }
                     //     while (respostacad == 1)
                 //   }
                 //   else
                      //  printf ("numero de funcionarios maximo atingido");

    return 0;

 }
