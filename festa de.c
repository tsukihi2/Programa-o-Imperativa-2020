#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int  conv;
    float extrasva =0;
    int extraconv = 0;

  scanf ("%d",&conv);
    while (conv >= 0);
    {
    scanf ("%d",&conv);
    extraconv = conv + extraconv;

    //if (conv >= 15 && conv <= 25)
    //{
        //extrasva = (conv -15) * 42;

    //}
    //extraconv = 1 + conv;

    }
    printf ("conv eh %d",extraconv);
    printf("conv negativo");

   	return 0;
}
